/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // By default Next.js will run in the root of the project; because this app lives in
  // a subdirectory of a mono repo, no additional configuration is required.
};

module.exports = nextConfig;